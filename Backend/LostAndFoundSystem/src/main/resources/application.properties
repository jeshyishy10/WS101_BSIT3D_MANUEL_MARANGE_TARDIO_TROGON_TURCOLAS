# Application
spring.application.name=LostAndFoundSystem
server.port=8082
# REMOVE THIS LINE or comment it out:
# server.servlet.context-path=/LostAndFoundSystem

# Database - keep as is
spring.datasource.url=jdbc:mysql://localhost:3306/lost_and_found_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=jessel1025
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate - keep as is
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# CORS Configuration
cors.allowed-origins=http://localhost:3000
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true

# Logging - add more detailed logging
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.boot.autoconfigure=DEBUG
logging.level.lk.ijse.cmjd108=TRACE
logging.level.org.hibernate.SQL=DEBUG

# Temporary - disable security for testing
# UNCOMMENT THIS to disable Spring Security temporarily:
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
management.endpoints.web.exposure.include=mappings,health
management.endpoint.health.show-details=always